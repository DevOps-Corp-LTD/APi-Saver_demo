# APi-Saver - Demo Version Environment Configuration
# Copy this file to .env - it works out of the box for demo!
# No changes are required to get started, although making adjustments is recommended.


# ============================================
# REQUIRED: Database Connection
# ============================================
# These MUST be in .env - needed to connect to PostgreSQL before app starts
# Cannot be configured in UI

POSTGRES_DB=apisaver
POSTGRES_USER=apisaver
POSTGRES_PASSWORD=apisaver-demo-password

# Optional: Database connection details (defaults work for Docker Compose)
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# ============================================
# REQUIRED: Security Secrets
# ============================================
# These MUST be in .env - used at startup to encrypt/decrypt database configs
# Cannot be configured in UI
# Generate strong secrets:
#   JWT_SECRET: openssl rand -base64 32
#   ENCRYPTION_KEY: openssl rand -hex 32

JWT_SECRET=0123456789abcdef0123456789abcdef0123456789=
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ============================================
# REQUIRED: Server Infrastructure
# ============================================
# These MUST be in .env - needed before server starts
# Cannot be configured in UI

NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# CORS origins (comma-separated) - needed at server startup
CORS_ORIGINS=http://localhost:5173,http://localhost:80,http://frontend

# ============================================
# Optional: Domain & TLS (Production only)
# ============================================
# These settings are ONLY needed for production deployments with HTTPS/SSL.
# Leave them commented out for local development and demo environments.
#
# What they do:
#   - DOMAIN: Your public domain name (e.g., api-saver.example.com)
#             Used by nginx and Let's Encrypt to generate SSL certificates
#   - EMAIL: Contact email for Let's Encrypt certificate expiration notices
#            Must be a valid email address you can access
#
# When to use:
#   - Production deployments requiring HTTPS
#   - Public-facing installations
#   - When using the included SSL certificate generation scripts
#
# When NOT to use:
#   - Local development (localhost)
#   - Demo/testing environments
#   - Internal-only deployments without public domain
#
# Example for production:
#   DOMAIN=api-saver.yourcompany.com
#   EMAIL=admin@yourcompany.com
#
# Note: If these are set, the nginx configuration will automatically:
#   - Generate SSL certificates via Let's Encrypt
#   - Configure HTTPS redirects
#   - Set up secure headers
#
# Uncomment and set for production HTTPS:
# DOMAIN=your-domain.com
# EMAIL=admin@your-domain.com
DOMAIN=localhost
DATABASE_URL=postgres://apisaver:apisaver-demo-password@postgres:5432/apisaver
